package com.example.food

import android.app.Activity

interface CellClickListener {
        fun onCellClickListener(id:String,collection:String)
        fun onCellClickListener1(id: String,collection: String)

}